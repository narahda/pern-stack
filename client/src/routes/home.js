import React from "react";
import AddUser from "../components/CreateUser";
import owlet from "../zrandom things/owlet.jpg";

const Home = () => {
    return(
        <div>
            hi im starting to lose sight of what im supposed to do in the project so ig this is just gonna be a placeholder meanwhile have a photo
            <img src ={owlet} width="1000" height= "auto"/>
        </div>
    )
};

export default Home;